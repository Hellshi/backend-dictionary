import { Column, Entity } from 'typeorm';
import { BaseEntity } from '../../database/common/baseEntity';

@Entity('{{snakeCase name}}')
export class {{ pascalCase name }} extends BaseEntity<{{ pascalCase name }}> {
  @Column()
  exampleColumn: string;
}
